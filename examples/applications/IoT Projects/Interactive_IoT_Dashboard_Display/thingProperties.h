// Code generated by Arduino IoT Cloud, DO NOT EDIT.
// This file contains the automatically generated properties and callbacks for your Thing.

#include <ArduinoIoTCloud.h>
#include <Arduino_ConnectionHandler.h>

// Your WiFi credentials (SSID and password) are defined in arduino_secrets.h
// These are automatically included by ArduinoIoTCloud.h
const char SSID[]     = SECRET_SSID;
const char PASS[]     = SECRET_OPTIONAL_PASS;

// Forward declarations for your callback functions
// No callbacks needed as all properties are READ-only for this logger

// Cloud variables
float temperature;     // Current ambient temperature (Celsius)
float lightLevel;      // Current ambient light level (e.g., 0-1023 or 0-100%)
char currentTime[20];  // Current time string (e.g., "HH:MM:SS")
int wifiStrength;      // Current WiFi signal strength (RSSI)

void initProperties(){

  // Initialize Cloud properties
  ArduinoCloud.addProperty(temperature, READ, ON_CHANGE, NULL);
  ArduinoCloud.addProperty(lightLevel, READ, ON_CHANGE, NULL);
  ArduinoCloud.addProperty(currentTime, READ, ON_CHANGE, NULL);
  ArduinoCloud.addProperty(wifiStrength, READ, ON_CHANGE, NULL);

}

// Initialize the WiFi connection handler
WiFiConnectionHandler ArduinoIoTPreferredConnection(SSID, PASS);
