// Code generated by Arduino IoT Cloud, DO NOT EDIT.
// This file contains the automatically generated properties and callbacks for your Thing.

#include <ArduinoIoTCloud.h>
#include <Arduino_ConnectionHandler.h>

// Your WiFi credentials (SSID and password) are defined in arduino_secrets.h
// These are automatically included by ArduinoIoTCloud.h
const char SSID[]     = SECRET_SSID;
const char PASS[]     = SECRET_OPTIONAL_PASS;

// Forward declarations for your callback functions
// No callbacks needed as all properties are READ-only for this monitor

// Cloud variables
bool slotStatus;   // Indicates if the parking slot is VACANT (true) or PARKED (false).
int distanceCm;    // The measured distance to an obstacle in the parking slot (in cm).

void initProperties(){

  // Initialize Cloud properties
  ArduinoCloud.addProperty(slotStatus, READ, ON_CHANGE, NULL);
  ArduinoCloud.addProperty(distanceCm, READ, ON_CHANGE, NULL);

}

// Initialize the WiFi connection handler
WiFiConnectionHandler ArduinoIoTPreferredConnection(SSID, PASS);
