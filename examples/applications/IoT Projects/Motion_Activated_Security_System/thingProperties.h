// Code generated by Arduino IoT Cloud, DO NOT EDIT.
// This file contains the automatically generated properties and callbacks for your Thing.

#include <ArduinoIoTCloud.h>
#include <Arduino_ConnectionHandler.h>

// Your WiFi credentials (SSID and password) are defined in arduino_secrets.h
// These are automatically included by ArduinoIoTCloud.h
const char SSID[]     = SECRET_SSID;
const char PASS[]     = SECRET_OPTIONAL_PASS;

// Forward declarations for your callback functions
void onSystemArmedChange();
void onResetAlarmChange();

// Cloud variables
bool motionDetected;  // Indicates if motion is currently detected. (Read-only)
bool alarmActive;     // Shows if the security alarm is currently sounding. (Read-only)
bool systemArmed;     // To arm or disarm the security system remotely. (Read & Write)
bool resetAlarm;      // A switch to remotely reset the alarm. (Read & Write)

void initProperties(){

  // Initialize Cloud properties and link them to their respective callbacks
  ArduinoCloud.addProperty(motionDetected, READ, ON_CHANGE, NULL);
  ArduinoCloud.addProperty(alarmActive, READ, ON_CHANGE, NULL);
  ArduinoCloud.addProperty(systemArmed, READWRITE, ON_CHANGE, onSystemArmedChange);
  ArduinoCloud.addProperty(resetAlarm, READWRITE, ON_CHANGE, onResetAlarmChange);

}

// Initialize the WiFi connection handler
WiFiConnectionHandler ArduinoIoTPreferredConnection(SSID, PASS);
